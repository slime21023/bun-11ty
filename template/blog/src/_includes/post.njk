{% extends "base.njk" %}

{% block content %}
<article>
  <header>
    <h1>{{ title }}</h1>
    <p>
      <time datetime="{{ date | date('YYYY-MM-DD') }}">{{ date | date('MMMM DD, YYYY') }}</time>
    </p>
  </header>
  <section>
    {{ content | safe }}
  </section>
</article>
{% endblock %}
